`
function love.draw()
	| test | asteroid1:draw()
	| -- | asteroid2:draw()
	| -- | asteroid3:draw()

	-- We're not rotating the ship, so no need for offset
	love.graphics.draw( shipImage, shipX, shipY )
end
`
